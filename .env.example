# ============================================================================
# Paperless-ngx Configuration for pngx-cao
# ============================================================================
# Copy this file to .env and fill in your values

# Required: Your Paperless-ngx instance URL
PAPERLESS_URL=http://your-paperless-server:8000

# Required: Authentication - Use either token (recommended) or username/password
# Get your token from Paperless-ngx: Settings → My Profile → Generate API Token
PAPERLESS_TOKEN=your-api-token-here

# Alternative: Username/Password authentication (if not using token)
# PAPERLESS_USERNAME=your-username
# PAPERLESS_PASSWORD=your-password

# ============================================================================
# Optional Configuration
# ============================================================================

# Environment prefix for managing multiple Paperless-ngx instances
# If set to "BOX1_", the tool will look for BOX1_PAPERLESS_URL, BOX1_PAPERLESS_TOKEN, etc.
# Leave empty for no prefix (recommended for single instance)
# ENV_PREFIX=

# Permission defaults - controls document and tag ownership
# Set to 'true' for global read access (no owner = everyone can read)
# Set to 'false' to assign ownership to the creator (default: false)
# Example: Setting to 'true' for global read access.
PAPERLESS_GLOBAL_READ=true

# SSL Certificate Verification
# Set to 'true' to skip SSL certificate verification (INSECURE - use only for development/self-signed certs)
# Set to 'false' for secure HTTPS connections (default: false, recommended for production)
# PAPERLESS_SKIP_SSL_VERIFY=false

# Duplicate Document Handling
# Controls what happens when uploading a document that already exists (matched by title)
# Options:
#   skip            - Skip uploading if document exists (default, safest)
#   replace         - Delete existing document and upload new version (DESTRUCTIVE)
#   update-metadata - Keep existing file, update only tags/metadata
# Note: CLI --duplicate-handling flag overrides this setting
# PAPERLESS_DUPLICATE_HANDLING=skip

# Directory Paths
# Path to directory containing taxonomy CSV files (actors.csv, motivations.csv, etc.)
# PAPERLESS_DATA_DIR=./data

# Path to directory containing document folders for upload
# PAPERLESS_ORIGINALS_DIR=./originals

# API version (rarely needs changing, default: 9)
# PAPERLESS_API_VERSION=9

# ============================================================================
# Usage with pngx-cao CLI
# ============================================================================
# After configuring this file:
#   1. Copy to .env:  cp .env.example .env
#   2. Edit .env with your actual values
#   3. Test connection: pngx-cao taxonomy list
#   4. Create tags:    pngx-cao taxonomy create --all
#   5. Upload docs:    pngx-cao upload batch ./originals
